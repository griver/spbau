#include <iostream>
#include <cstdio>
using namespace std;


int exp_17(int x) {
    int y = 1;
    y *= x;
    x *= x;
    x *= x;
    x *= x;
    x *= x;
    y *= x;
    return y;
}


int main() {
    int x;
    cout << "write x we power it in 5 степень: " << endl;
    cin >> x;
    cout << "exp_n(x): " << exp_17(x) << endl;
    return 0;
}
